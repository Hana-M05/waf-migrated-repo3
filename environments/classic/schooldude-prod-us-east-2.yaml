#################################################################
# WAF Security Configuration
# 
# This file controls what gets blocked and what security 
# protections are enabled for your website/application.
#
# ACTIONS EXPLAINED:
# - "block": Completely blocks the request (recommended for production)
# - "count": Logs the request but allows it through (good for testing)
# - "allow": Explicitly allows the request (rarely used)
#################################################################

#################################################################
# General Configurations
# 
# A set of general configurations for the WAF, including
# the ALB name, environment, and region.
#################################################################
alb_arns: [
  "arn:aws:elasticloadbalancing:us-east-2:989263075007:loadbalancer/app/classic-sd-prod-external-alb/b2d576d6a8e4c838"
]
api_gateway_ids: []
global: false
redacted_headers: []
region: "us-east-2"
waf_error_subscribers:
  - "sam.mcmanus@siemens.com"
  - "productops@brightlysoftware.com"

#################################################################
# SECURITY PROTECTION RULES
# 
# Configure different types of automated security protections.
# Each rule can be enabled/disabled and set to block, count, or allow.
#################################################################
protection_rules:
  #################################################################
  # IP BLOCKING
  # 
  # Block specific IP addresses from accessing your website.
  # Add IP addresses in CIDR format (use /32 for single IPs).
  #################################################################
  ip_blocking:
    enabled: true
    action: "block"
    ips:
      - "184.168.104.171/32"
      - "52.114.128.147/32"
      - "123.20.29.193/32"
      - "150.129.80.32/32"
      - "209.58.168.85/32"
      - "52.27.98.123/32"
      - "52.10.54.112/32"
      - "54.191.229.196/32"

  #################################################################
  # CUSTOM PATH BLOCKING
  # 
  # Block access to specific parts of your website like admin
  # areas, internal APIs, or sensitive endpoints.
  #################################################################
  path_blocking:
    enabled: false
    action: "count"
    paths: []

  #################################################################
  # GEOLOCATION BLOCKING
  #
  # In block mode, creates a blacklist of countries
  # In allow mode, creates a whitelist of countries
  #################################################################
  geolocation_blocking:
    enabled: true
    action: "allow"
    countries: 
      - "AS"
      - "AU"
      - "BD"
      - "BM"
      - "BN"
      - "CA"
      - "CN"
      - "CU"
      - "CZ"
      - "DO"
      - "EG"
      - "FR"
      - "DE"
      - "GR"
      - "GU"
      - "IN"
      - "IT"
      - "JP"
      - "JO"
      - "MY"
      - "MX"
      - "NZ"
      - "PA"
      - "PT"
      - "PR"
      - "RO"
      - "ES"
      - "TN"
      - "AE"
      - "US"
      - "VE"
      - "VI"
      - "GB"

  #################################################################
  # RATE LIMITING
  # 
  # Limit the number of requests from a single IP to prevent
  # abuse or DDoS attacks. Customize rules as needed.
  #################################################################
  rate_limiting:
    enabled: false
    rules: []

  #################################################################
  # RATE LIMITING
  # 
  # Limit the number of requests from a single IP to prevent
  # abuse or DDoS attacks. Customize rules as needed.
  #################################################################
  captcha:
    enabled: false
    rules: []

  #################################################################
  # BLOCK UNAUTHORIZED SCANNERS
  # 
  # Blocks requests from known vulnerability scanners that are
  # not used by Brightly.
  #################################################################
  block_unauthorized_scanners:
    enabled: true
    action: "block"

  #################################################################
  # AWS MANAGED SECURITY RULES
  # 
  # These are pre-built security rules provided by AWS that
  # protect against common attacks and threats.
  #################################################################
  basic_protection:
    enabled: true
    action: "block"
    version: "Version_1.21"

  malicious_requests:
    enabled: true
    action: "block"
    version: "Version_1.25"
    
  sql_injection:
    enabled: true
    action: "block" 
    version: "Version_1.3"

  windows_protection:
    enabled: true
    action: "block"
    version: "Version_2.4"

  linux_protection:
    enabled: false
    action: "block"
    version: "Version_2.6"

  ip_reputation:
    enabled: true
    action: "block"

#################################################################
# DISABLE SPECIFIC SECURITY RULES
# 
# If legitimate traffic is being blocked by AWS managed rules,
# you can disable specific rules here. Only do this if you 
# understand the security implications.
# 
# Note: This only applies to AWS managed rules, not custom
# IP or path blocking rules.
#################################################################
disabled_rules:
  basic_protection: 
    - "NoUserAgent_HEADER" # Triggered frequently
    - "SizeRestrictions_QUERYSTRING" # Triggered on work order updates and MyScheduleNew endpoints
    - "SizeRestrictions_Cookie_HEADER" # Triggered when large cookies are generated
    - "SizeRestrictions_BODY" # Triggered during file uploads
    - "GenericLFI_QUERYARGUMENTS" # Legacy override - All events blatantly malicious
    - "GenericLFI_BODY" # Triggered during work order updates
    - "CrossSiteScripting_QUERYARGUMENTS" # Triggered by the alert function
    - "CrossSiteScripting_BODY" # Triggered on .svc endpoints
  malicious_requests: []
  sql_injection: 
    - "SQLiExtendedPatterns_QUERYARGUMENTS" # Legacy override - No events in last 7 days
    - "SQLi_QUERYARGUMENTS" # Triggered on work order updates, entries, and mygateway
    - "SQLi_BODY" # Triggered on a variety of endpoints 
    - "SQLi_COOKIE" # Triggered by genuine customer requests on some pages
    - "SQLi_URIPATH" # Legacy override - All events blatantly malicious
  windows_protection: 
    - "WindowsShellCommands_BODY" # Triggered on work order db updates and work order updates
    - "WindowsShellCommands_QUERYARGUMENTS" # Triggered on work order updates and FDMarketing searches
  linux_protection: [] 
  ip_reputation: []